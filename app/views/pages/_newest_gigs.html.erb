<div class="container section">
  <h1>
    Newest Gigs
  </h1>
  <div class="row">
    <% @services.each do |service| %>
      <%= render partial: 'shared/gigcard', locals: { service: service } %>
    <% end %>
  </div>
</div>
